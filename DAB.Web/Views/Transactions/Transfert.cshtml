@model DAB.Web.Models.Transaction
@{
    ViewData["Title"] = "New Transfer";
    var sourceCompte = ViewBag.SourceCompte as DAB.Web.Models.Compte;
    var destinationComptes = ViewBag.DestinationComptes as List<DAB.Web.Models.Compte> ?? new List<DAB.Web.Models.Compte>();
}

<div class="row min-vh-100 align-items-center justify-content-center">
    <div class="col-12 col-md-8 col-lg-6 col-xl-5">
        <div class="fintech-card p-5 mt-n5 border-top border-4 border-primary">
            <div class="text-center mb-4">
                <div class="bg-primary bg-opacity-10 rounded-circle d-inline-flex p-3 mb-3 mx-auto">
                    <i class="bi bi-send text-primary fs-2"></i>
                </div>
                <h3 class="fw-bold text-dark">Send Money</h3>
                <p class="text-muted">Instant transfer between accounts</p>
            </div>

            <form asp-action="Transfert" method="post">
                <input type="hidden" asp-for="CompteId" />
                <div class="mb-4">
                    <label class="form-label text-muted fw-semibold small text-uppercase">Source Account</label>
                    <div class="fintech-card bg-light border-0 p-3">
                        <div class="fw-bold text-dark">@sourceCompte?.NumeroCompte</div>
                        <div class="text-muted small">@sourceCompte?.Proprietaire</div>
                        @if (sourceCompte != null)
                        {
                            <div class="text-muted small">Balance: € @sourceCompte.Solde.ToString("N2")</div>
                        }
                    </div>
                </div>

                <!-- Destination Account -->
                <div class="mb-4">
                    <label class="form-label text-muted fw-semibold small text-uppercase" asp-for="NumeroCompteDestination">Destination Account</label>
                    <select asp-for="NumeroCompteDestination" class="form-select form-select-lg bg-light border-0 shadow-none" required>
                        <option value="">-- Select destination account --</option>
                        @foreach (var c in destinationComptes)
                        {
                            <option value="@c.NumeroCompte">@c.NumeroCompte - @c.Proprietaire (€ @c.Solde.ToString("N2"))</option>
                        }
                    </select>
                </div>

                <!-- Amount -->
                <div class="mb-5">
                    <label class="form-label text-muted fw-semibold small text-uppercase" asp-for="Montant">Amount</label>
                    <div class="input-group input-group-lg">
                        <span class="input-group-text border-0 bg-light fw-bold text-dark">€</span>
                        <input asp-for="Montant" type="number" class="form-control border-0 bg-light shadow-none fw-bold fs-4" placeholder="0.00" min="1" step="0.01" required>
                    </div>
                </div>

                <!-- Submit Button -->
                <button type="submit" class="btn btn-primary btn-lg w-100 fw-bold shadow-sm p-3">
                    <i class="bi bi-arrow-right-circle me-2"></i>Proceed to Transfer
                </button>
                <div class="text-center mt-3">
                    <a href="/Transactions" class="text-muted text-decoration-none small">Cancel & Go Back</a>
                </div>
            </form>
        </div>
    </div>
</div>
